# Your phone can't reach "localhost" of your computer — use your computer's
# LAN IP instead (e.g. run `ipconfig` on Windows / `ifconfig` on Mac and find
# your local network address), and make sure your phone is on the same WiFi.
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.100:3000
